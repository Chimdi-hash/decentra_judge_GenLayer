# conftest.py — root-level pytest configuration for genlayer-test Direct Mode.
# The genlayer-test package registers direct_vm, direct_deploy, direct_alice,
# direct_bob, direct_owner, and direct_accounts as built-in fixtures automatically
# when installed, so no manual fixture definition is needed here.
#
# Install the test framework:
#   pip install genlayer-test
#
# Run all tests:
#   pytest tests/ -v
